IDENTIFICATION DIVISION.
PROGRAM-ID. BAD-CODE.

DATA DIVISION.
WORKING-STORAGE SECTION.
01  WS-VARIABLE-NAME            PIC X(10) VALUE "INITIALIZED".
01  WS-UNUSED-VARIABLE         PIC X(10).
01  WS-DUPLICATED-CODE         PIC X(10).

PROCEDURE DIVISION.
    PERFORM INITIALIZE-VARIABLES
    PERFORM PROCESS-DATA
    PERFORM DISPLAY-RESULTS
    GOBACK
   .

INITIALIZE-VARIABLES.
    MOVE "INITIALIZED" TO WS-VARIABLE-NAME
    MOVE "UNUSED" TO WS-UNUSED-VARIABLE
   .

PROCESS-DATA.
    PERFORM PROCESS-DATA-INTERNAL 3 TIMES
    PERFORM PROCESS-DATA-INTERNAL 3 TIMES
   .

PROCESS-DATA-INTERNAL.
    ADD 1 TO WS-VARIABLE-NAME
    IF WS-VARIABLE-NAME = "INITIALIZED"
        MOVE "DUPLICATED" TO WS-DUPLICATED-CODE
    END-IF
   .

DISPLAY-RESULTS.
    DISPLAY "RESULTS: " WS-VARIABLE-NAME
    DISPLAY "UNUSED VARIABLE: " WS-UNUSED-VARIABLE
    DISPLAY "DUPLICATED CODE: " WS-DUPLICATED-CODE
   .